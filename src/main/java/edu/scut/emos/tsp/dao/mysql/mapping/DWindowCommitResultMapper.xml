<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.scut.emos.tsp.dao.mysql.DWindowCommitResultMapper">
	<resultMap id="BaseResultMap" type="edu.scut.emos.tsp.model.DWindowCommitResult">
		<id column="commitresultid" property="commitresultid" jdbcType="VARCHAR" />
		<result column="committime" property="committime" jdbcType="DATE" />
	</resultMap>
	<sql id="Base_Column_List">
		commitresultid, committime
	</sql>
	<select id="selectByPrimaryKey" resultMap="BaseResultMap"
		parameterType="java.lang.String">
		select
		<include refid="Base_Column_List" />
		from t_window_commitresult
		where commitresultid =
		#{commitresultid,jdbcType=VARCHAR}
	</select>
	<delete id="deleteByPrimaryKey" parameterType="java.lang.String">
		delete from
		t_window_commitresult
		where commitresultid =
		#{commitresultid,jdbcType=VARCHAR}
	</delete>
	<insert id="insert" parameterType="edu.scut.emos.tsp.model.DWindowCommitResult">
		insert into
		t_window_commitresult (commitresultid, committime)
		values
		(#{commitresultid,jdbcType=VARCHAR}, #{committime,jdbcType=DATE})
	</insert>
	<insert id="insertSelective" parameterType="edu.scut.emos.tsp.model.DWindowCommitResult">
		insert into t_window_commitresult
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="commitresultid != null">
				commitresultid,
			</if>
			<if test="committime != null">
				committime,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="commitresultid != null">
				#{commitresultid,jdbcType=VARCHAR},
			</if>
			<if test="committime != null">
				#{committime,jdbcType=DATE},
			</if>
		</trim>
	</insert>
	<update id="updateByPrimaryKeySelective" parameterType="edu.scut.emos.tsp.model.DWindowCommitResult">
		update t_window_commitresult
		<set>
			<if test="committime != null">
				committime = #{committime,jdbcType=DATE},
			</if>
		</set>
		where commitresultid = #{commitresultid,jdbcType=VARCHAR}
	</update>
	<update id="updateByPrimaryKey" parameterType="edu.scut.emos.tsp.model.DWindowCommitResult">
		update
		t_window_commitresult
		set committime = #{committime,jdbcType=DATE}
		where commitresultid = #{commitresultid,jdbcType=VARCHAR}
	</update>

	<!-- 添加方法 -->
	<insert id="insertAll" parameterType="java.util.List">
		insert into t_window_commitresult (commitresultid, committime)
		values
		<foreach collection="list" item="record" index="index"
			separator=",">
			(#{record.commitresultid,jdbcType=VARCHAR},
			#{record.committime,jdbcType=DATE})
		</foreach>
	</insert>
</mapper>